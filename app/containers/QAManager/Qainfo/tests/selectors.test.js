// import { fromJS } from 'immutable';
// import { makeSelectQainfoDomain } from '../selectors';

// const selector = makeSelectQainfoDomain();

describe('makeSelectQainfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
