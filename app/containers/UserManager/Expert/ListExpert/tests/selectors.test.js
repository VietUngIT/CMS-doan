// import { fromJS } from 'immutable';
// import { makeSelectListExpertDomain } from '../selectors';

// const selector = makeSelectListExpertDomain();

describe('makeSelectListExpertDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
